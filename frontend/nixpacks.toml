# Let Nixpacks auto-detect Node.js version from package.json engines

[phases.install]
cmds = ['npm ci --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[variables]
NODE_ENV = 'production'
NEXT_TELEMETRY_DISABLED = '1'

[start]
cmd = 'npm start'
