[build]
builder = "nixpacks"
watchPatterns = ["frontend/**"]

[build.nixpacks]
buildCmd = "cd frontend && npm ci && npm run build"

[deploy]
startCommand = "cd frontend && npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
